on: [push]

jobs: 
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo-a-string
        run: echo "Welcome to github actions"
      - name: working-dir
        run: pwd
      - name: multi-line
        run: |
          echo $SHELL
          pwd
          whoami
          java --version
      
